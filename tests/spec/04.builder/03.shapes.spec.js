/*
 * Copyright (c) 2011-2013 by Animatron.
 * All rights are reserved.
 *
 * Animatron player is licensed under the MIT License, see LICENSE.
 */

xdescribe("", function() {

    xit("", function() {

    });

    // TODO: tests that inaccessible image throws exception

    // TODO: test changing shapes live (while drawing) actually affects the drawing process

    // TODO: test text is ok to handle numbers and multiple lines:
    /*    var b = Builder._$, C = anm.C;

          var scene1 = b().text([200, 100], "String Text");
          var p1 = createPlayer('canvas1').load(scene1);

          var scene2 = b().text([200, 100], 5);
          var p2 = createPlayer('canvas2').load(scene2);

          var scene3 = b().text([200, 100], ["test","with","multi-line"]);
          var p3 = createPlayer('canvas3').load(scene3);

          var scene4 = b().text([200, 100], [1,2,3]);
          var p4 = createPlayer('canvas4').load(scene4);
    */

});